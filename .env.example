# Copy to .env for local development. NEVER commit .env.
# The key is read only on the server (Vite dev middleware locally, Vercel Functions in production).
# It is never bundled into the browser code.

GEMINI_API_KEY="paste-your-key-from-aistudio.google.com"

# Optional: override the model used for analysis and drafting.
# GEMINI_MODEL="gemini-2.5-flash"
